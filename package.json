{
  "name": "aws-exam-generator",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/shared",
    "packages/mcp-server",
    "packages/backend",
    "packages/frontend",
    "packages/aws-cloud"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "node ./scripts/walden-test-runner.mjs"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
